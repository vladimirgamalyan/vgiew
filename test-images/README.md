# Test images

Scratch folder for images used while manually testing vgiew (opening,
browsing, single-instance window reuse, etc.).

The image files here are **git-ignored** — drop whatever local samples you
need; they are never committed. Only this `README.md` is tracked, so the
folder itself stays in the repo.
